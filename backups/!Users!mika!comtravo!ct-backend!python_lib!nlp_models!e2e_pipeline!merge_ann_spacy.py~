import spacy
from email_tagger.merge_annotations import merge_spacy_with_annotations


for r in raw_data:
    body_txt = raw_data['pred_ann']['analyzes']['body']['text']
    # only cut at end
    body_txt = body_txt[:raw_data['pred_ann']['analyzes']['body']['end']]
    subject_txt = raw_data['pred_ann']['analyzes']['subject']['text']
    if raw_data['ann']['lanugage'] == 'de':
        sp = nlp_de(raw_data['ann'[
