From AMI here https://aws.amazon.com/marketplace/pp/B00FYCDDTE?qid=1477650388732&sr=0-1&ref_=srh_res_product_title


sudo yum update
sudo yum install python35 -y
sudo yum install python35-pip -y
sudo yum install python35-devel -y
sudo yum install gcc-gfortran -y
sudo yum install atlas-devel -y
sudo yum install emacs -y

sudo update-alternatives --config python

sudo pip install numpy
sudo pip install scipy
sudo pip install nose
sudo pip install Theano
sudo pip install regex
sudo pip install keras

echo "export THEANO_FLAGS='cuda.root=/opt/nvidia/cuda,device=gpu,floatX=float32,blas.ldflags=-lf77blas -latlas -lgfortran'" | tee -a .bashrc
echo "export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/opt/nvidia/cuda/lib:/opt/nvidia/cuda/lib64" | tee -a .bashrc
echo "export KERAS_BACKEND=theano" | tee -a .bashrc
