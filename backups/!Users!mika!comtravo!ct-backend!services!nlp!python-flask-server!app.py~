#!/usr/bin/env python3

import connexion

if __name__ == '__main__':
    app = connexion.App(__name__, specification_dir='..')
    app.add_api('nlp.yaml', arguments={'title': 'Provides an API to parse information from a message'},
                validate_responses=True)
    app.run(port=8080)
