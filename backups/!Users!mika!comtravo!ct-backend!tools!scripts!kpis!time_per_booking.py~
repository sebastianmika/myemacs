import pyodbc
import pandas as pd


conn = pyodbc.connect("DSN=MapR Drill DSN", autocommit=True)
cursor = conn.cursor()

q = "SELECT DISTINCT(ticketId) ticketId, COUNT(*) cnt_updated FROM `ticket-prod` GROUP BY ticketId"
cursor.execute(q)
data = pd.DataFrame(dict(cursor.fetchall())
cursor.close()
