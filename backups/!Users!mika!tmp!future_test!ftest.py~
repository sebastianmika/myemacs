from future.concurrent import ThreadPoolExecutor, as_completed


def task(r, v):
    r['result_{}'.format(v)] = v
    return True


tpe = ThreadPoolExecutor(4)
rr = {}
ths = [tpe.submit(task, rr, i) for i in range(10)]

for f in as_completed(ths):
    print("DONE: ", f.result()
