import pandas as pd


class BaseTarget:
    def search(self):
        return "Constant"

   @ttl_cache(maxsize=32, ttl=60)
   def query(self, from_, to_, freq):
        ts = pd.date_range(from_, to_, freq=freq)
        return [(1, t) for t in ts]
